spring:
  application:
    name: "config-server"
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/OguzAlpCepni/BankManagementSystemConfig.git"
          default-label: main
          clone-on-start: true
          skip-ssl-validation: true
server:
  port: 8079